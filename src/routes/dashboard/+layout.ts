/**
 * Dashboard Layout
 *
 * Note: Auth protection is handled client-side in the page component
 * using onMount(), not here. This is because localStorage is only
 * available in the browser, not during server-side rendering.
 */

export function load() {
  return {};
}
